task docsZip(type: Zip) {
    //baseName = "project"
    destinationDir = file("build")
    appendix = "docs"
    classifier = "jdk15"
    from("src/test/groovy") {
        include "**/*.groovy"
    }
}
